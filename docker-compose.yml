version: '3.8'

services:
  ai-tool-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.local
    volumes:
      - ./debug_output:/app/debug_output
    restart: "no"
    shm_size: '2gb'
    command: python ai_crawling_automation.py 